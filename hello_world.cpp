
//We're including. the iostream library, which allows us to use cout
#include <iostream>

int main() {
    
    //std::cout prints the content inside the quotation marks. It must be followed by << followed by the format string "".
    std::cout << "Hello World!";

    //The return 0; statement is the "Exit status", with 0 indicating that we're not returning anything.
    return 0;
}